print-alias-src() {
  echo $1 := `alias $1 | sed "s/^.*='//;s/'$//"`
}

cors-chrome() {
  echo "Launching Chrome with CORS disabled..."
  open -na Google\ Chrome --args --user-data-dir=/tmp/temporary-chrome-profile-dir --disable-web-security --disable-site-isolation-trials
}
